import { $uuid, $spoof } from "rbxts-transformer-spoof";

print($spoof("main.client.ts"));

export enum cookies_spoof {
	chocolateChip,
	oatmeal,
	snickerdoodle,
}

print(cookies_spoof.chocolateChip);
